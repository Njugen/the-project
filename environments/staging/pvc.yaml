apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-storage-claim-staging
spec:
  storageClassName: todo-staging-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi