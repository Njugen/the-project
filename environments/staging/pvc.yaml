apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-staging-storage-claim
spec:
  storageClassName: todo-staging-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi