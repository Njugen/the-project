apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: todo-storage-claim
spec:
  storageClassName: todo-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi